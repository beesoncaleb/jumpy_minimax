#include <stdio.h>

#define BOARD_LEN 4
// Computer move
int comp_move[4] = {-1, -1, -1, -1 };

int static_estimator(int[]);
int compute_win(int[]);
int minimax(int[], int, int, int, int);

int minimax(int board[], int alpha, int beta, int depth, int max_depth) {
  return 0;
};

int main() {
  return 0;
};
